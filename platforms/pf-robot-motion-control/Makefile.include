USEMODULE_INCLUDES_pf-robot-motion-control := $(LAST_MAKEFILEDIR)/include
USEMODULE_INCLUDES += $(USEMODULE_INCLUDES_pf-robot-motion-control)

PROTOBUF_PATH_pf-robot-motion-control := $(LAST_MAKEFILEDIR)
PROTOBUF_PATH += $(PROTOBUF_PATH_pf-robot-motion-control)

ifdef PCA9685_OFFSET
	CFLAGS += -DPCA9685_OFFSET=$(PCA9685_OFFSET)
endif

ifeq ($(CPU),native)
	CFLAGS += -DUART_NUMOF=2
endif
