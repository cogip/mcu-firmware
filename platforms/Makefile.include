LAST_MAKEFILEDIR_platforms := $(LAST_MAKEFILEDIR)

# Calibration
EXTERNAL_MODULE_DIRS += $(LAST_MAKEFILEDIR_platforms)/shell_pf

# Platforms
EXTERNAL_MODULE_DIRS += $(LAST_MAKEFILEDIR_platforms)/cortex
