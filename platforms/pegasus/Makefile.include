USEMODULE_INCLUDES_pegasus := $(LAST_MAKEFILEDIR)/include
USEMODULE_INCLUDES += $(USEMODULE_INCLUDES_pegasus)

PROTOBUF_PATH_pegasus := $(LAST_MAKEFILEDIR)
PROTOBUF_PATH += $(PROTOBUF_PATH_pegasus)

ifdef PCA9685_OFFSET
	CFLAGS += -DPCA9685_OFFSET=$(PCA9685_OFFSET)
endif

ifeq ($(CPU),native)
	CFLAGS += -DUART_NUMOF=2
endif
